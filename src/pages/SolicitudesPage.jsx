import { Typography } from "@mui/material";

const SolicitudesPage = ({ theme }) => {
  return <Typography variant='h4' color={theme.color.primary}>Solicitudes</Typography>;
}

export default SolicitudesPage;